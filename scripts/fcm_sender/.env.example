# Example env for scripts/fcm_sender
# Option 1: point to service account JSON
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Option 2: embed service account JSON (not recommended for production)
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'
