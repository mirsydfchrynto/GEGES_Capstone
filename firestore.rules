rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // MATCH KE SEMUA DOKUMEN (WILDCARD)
    match /{document=**} {
      // Izinkan baca dan tulis untuk siapa saja (Mode Test/Dev)
      allow read, write: if true;
    }
    
  }
}