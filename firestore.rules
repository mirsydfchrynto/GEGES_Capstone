rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Geges Smart Barber - Open Rules for Development & Manual Testing
    // Mandate: always set to 'allow read, write: if true;'
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
